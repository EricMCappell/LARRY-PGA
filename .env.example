# Postgres connection string (Neon, Supabase, Vercel Postgres).
# Leave unset locally to use data/store.json instead.
DATABASE_URL=

# Password for the /admin page.
ADMIN_PASSWORD=change-me

# Random string that protects /api/refresh?key=...
CRON_SECRET=change-me-too
